Локальное P2P-приложение для обмена файлами через IPFS с веб-интерфейсом.  
Работает полностью автономно: IPFS-узел встроен напрямую в приложение (без зависимости от внешнего демона).  
Поддерживает обмен файлами за NAT благодаря встроенной P2P-сети IPFS.

## 🔧 Функционал

- **Загрузка файлов**: любой файл добавляется в локальный IPFS-узел и получает уникальный CID.
- **Скачивание по CID**: получатель может вставить CID и начать загрузку напрямую от пиров.
- **Ссылки на файлы**: после загрузки генерируется децентрализованная ссылка вида `ipfs://<CID>`, которую можно передать любому пользователю (даже за NAT). Получатель вставляет эту ссылку в своё приложение, и файл скачивается напрямую через P2P-сеть IPFS с использованием публичных relay-серверов для обхода NAT — без централизованных серверов и без привязки к localhost.
- **Мульти-пиринг**: как только второй пользователь скачал файл, он сам становится источником — последующие загрузки могут идти от обоих узлов (аналогично торренту).
- **Динамический UI**: интерфейс обновляется в реальном времени через WebSocket.
- **Информация о файлах**:
  - Имя файла
  - CID
  - Количество известных пиров
  - Прогресс скачивания (в процентах)
- **Flat-дизайн**: минималистичный, читаемый интерфейс без излишеств.

## 🖥️ Интерфейс

Веб-интерфейс доступен по адресу `http://localhost:3000`.

Основные элементы:
- Кнопка загрузки файла
- Сетка карточек с файлами
- Для каждого файла отображается:
  - Имя
  - CID
  - Количество пиров (👥)
  - Прогресс-бар скачивания
  - Поле с HTTP-ссылкой (однократный клик — копирование)

## 🏗️ Архитектура

Приложение построено по модульному принципу с чётким разделением ответственности:

```
internal/
├── http/       — инициализация Fiber, HTTP-обработчики, WebSocket-хаб
├── ipfs/       — встроенный IPFS-узел (go-ipfs-core)
├── store/      — локальное хранилище метаданных
types/          — общие типы (FileMetadata, FileUpdate)
views/          — HTML-шаблон интерфейса
```

Все модули покрыты unit- и интеграционными тестами.  
Общие типы вынесены в пакет `types`, чтобы избежать циклических импортов.

## 🛠️ Технологии

- **Go 1.21+**
- **Fiber** — высокопроизводительный веб-фреймворк
- **go-ipfs-core** — встроенный IPFS-стек
- **WebSocket** — для живого обновления UI

## 📦 Сборка

```bash
CGO_ENABLED=1 go build -o webipfs cmd/main.go
```

> **Важно**: сборка требует CGO, так как `go-ipfs-core` зависит от C-библиотек (например, для LevelDB).

## 🔒 Безопасность

- Приложение работает **только на localhost**.
- Нет аутентификации — предназначено для локального P2P-обмена между доверенными пользователями.
- Все данные хранятся во временном IPFS-репозитории (в памяти или во временной папке — в зависимости от конфигурации).
